export enum AppRoute {
  Login = '/login',
  Favorites = '/favorites',
  Main = '/',
  Property = '/offer',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NOAUTH',
  Unknown = 'UNKNOWN',
}

// test commit
